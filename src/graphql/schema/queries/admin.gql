# Administrative user queries
extend type Query {
    """List all administrator accounts that are not soft-deleted."""
    getAdmins: [Admin!]!
    """Fetch a single administrator by unique identifier."""
    getAdmin(id: ID!): Admin
}