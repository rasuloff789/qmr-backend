# Academic degree queries
extend type Query {
    """List every degree that can be assigned to teachers or courses."""
    getDegrees: [Degree!]!
    """Fetch a specific degree by unique identifier."""
    getDegree(id: ID!): Degree
}
