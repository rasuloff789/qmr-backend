type LoginResponse {
  success: Boolean!
  message: String
  token: String
  user: User
}

union User = Root | Admin

type Mutation {
  login(username: String!, password: String!, userType: String!): LoginResponse!
}

type Query {
  me: User
}